 alias ll='ls -l'
 alias la='ls -a'

case "$(uname -s)" in
   Darwin)
     # echo 'Mac OS X'
	alias ls='ls -G'
     ;;

   Linux)
	alias ls='ls --color=auto'
     ;;

   *)
     # echo 'Other OS' 
     ;;
esac
